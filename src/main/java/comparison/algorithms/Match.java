package comparison.algorithms;

public class Match {
    public int p_pos;
    public int s_pos;
    public int len;

    public Match(int p_pos, int s_pos, int len) {
        this.p_pos = p_pos;
        this.s_pos = s_pos;
        this.len = len;
    }

}